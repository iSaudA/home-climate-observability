# Contributing

Run `make validate` before submitting changes. Keep real secrets, Home Assistant `.storage`, databases, logs, backups, learned codes, device identifiers, and private network details out of Git. Validate policy changes through the Home Assistant configuration check, automation traces, and controlled hardware trials.
