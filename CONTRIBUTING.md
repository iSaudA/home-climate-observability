# Contributing

Run `make validate` before submitting changes. Keep real secrets, Home Assistant `.storage`, databases, logs, backups, learned codes, device identifiers, and private network details out of Git. Tests must not require physical devices. Changes to policy must update both the Home Assistant templates and the Python reference tests.
